export const metadata = {
  title: "Client Services | WITHUS",
  description: "Customer service and FAQ for WITHUS.",
};

export default function ClientServicesLayout({ children }) {
  return <>{children}</>;
}
